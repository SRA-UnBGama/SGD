<h1>Listing phases</h1>

<table>
  <thead>
    <tr>
      <th>Start date phase</th>
      <th>End date phase</th>
      <th>Status phase</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @phases.each do |phase| %>
      <tr>
        <td><%= phase.start_date_phase %></td>
        <td><%= phase.end_date_phase %></td>
        <td><%= phase.status_phase %></td>
        <td><%= link_to 'Show', phase %></td>
        <td><%= link_to 'Edit', edit_phase_path(phase) %></td>
        <td><%= link_to 'Destroy', phase, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Phase', new_phase_path %>
