<script type="text/javascript">
  $(document).ready(function(){
    $('[data-behaviour~=datepicker]').datepicker({
        format: 'dd/mm/yyyy',
        language: 'pt-BR'
    });
  })
</script>

<%= form_for(@phase) do |f| %>
  <% if @phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phase.errors.count, "error") %> prohibited this phase from being saved:</h2>

      <ul>
      <% @phase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date_phase %><br>
    <%= f.text_field :start_date_phase %>
  </div>
  <div class="field">
    <%= f.label :end_date_phase %><br>
    <%= f.text_field :end_date_phase %>
  </div>
   <div class="actions">
    <%= f.submit 'Definir Fase de Avaliação X', class:"btn btn-success" %>
  </div>
<% end %>
